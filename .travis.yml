language: python
sudo: false
branches:
  only:
    - travis
python:
  - "2.7"
  - "3.4"
addons:
  apt:
    packages:
#    - libblas-dev
#    - liblapack-dev
    - gfortran
    - python-numpy
    - python-scipy
install:
  - pip setup.py install
cache:
  - apt
  - pip
  - directories:
    - $HOME/.cache/pip
